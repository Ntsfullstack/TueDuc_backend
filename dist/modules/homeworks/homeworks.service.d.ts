import { Repository } from 'typeorm';
import { CurrentUserData } from '../../common/decorators/current-user.decorator';
import { Class } from '../classes/entities/class.entity';
import { Student } from '../students/entities/student.entity';
import { User } from '../users/entities/user.entity';
import { CreateHomeworkDto } from './dto/create-homework.dto';
import { GradeSubmissionDto } from './dto/grade-submission.dto';
import { SubmitQuizDto } from './dto/submit-quiz.dto';
import { UpdateHomeworkDto } from './dto/update-homework.dto';
import { Homework } from './entities/homework.entity';
import { HomeworkSubmission } from './entities/homework-submission.entity';
import { HomeworkTarget } from './entities/homework-target.entity';
export declare class HomeworksService {
    private readonly homeworkRepository;
    private readonly targetRepository;
    private readonly submissionRepository;
    private readonly classRepository;
    private readonly studentRepository;
    private readonly userRepository;
    private isRecord;
    private getQuizQuestions;
    constructor(homeworkRepository: Repository<Homework>, targetRepository: Repository<HomeworkTarget>, submissionRepository: Repository<HomeworkSubmission>, classRepository: Repository<Class>, studentRepository: Repository<Student>, userRepository: Repository<User>);
    private stripQuizAnswersIfNeeded;
    create(actor: CurrentUserData, dto: CreateHomeworkDto): Promise<Homework>;
    update(actor: CurrentUserData, id: string, dto: UpdateHomeworkDto): Promise<Homework>;
    findById(actor: CurrentUserData, id: string): Promise<Homework>;
    private isHomeworkVisibleToAnyStudent;
    list(actor: CurrentUserData, studentId?: string): Promise<Homework[]>;
    submitQuiz(actor: CurrentUserData, homeworkId: string, dto: SubmitQuizDto): Promise<HomeworkSubmission>;
    submitEssay(actor: CurrentUserData, homeworkId: string, studentId: string, attachmentPaths: string[]): Promise<HomeworkSubmission>;
    private isHomeworkVisibleToStudent;
    listSubmissions(actor: CurrentUserData, homeworkId: string): Promise<HomeworkSubmission[]>;
    gradeSubmission(actor: CurrentUserData, submissionId: string, dto: GradeSubmissionDto): Promise<HomeworkSubmission | null>;
    homeworkStatus(actor: CurrentUserData, homeworkId: string): Promise<{
        studentId: string;
        studentName: string;
        status: string;
        score: number | null;
        submissionId: string | null;
    }[]>;
}
